# 网页体验 V3（现役内部结构）

应用版本：`0.3.0`

正式启动：

```bash
python web_server.py
```

访问 <http://127.0.0.1:8787>。

V3 是当前结果展示层，但仍复用 V2 的输入与请求适配层。用户能力和限制以 [README.md](README.md) 为唯一权威说明。

## 0.3.0 变化摘要

- 顶部增加“命盘 · 观人 / 黄历 · 观时”双入口。
- 命盘增加章节导航，以及默认折叠的实验推演区。
- 实验推演通过稳定 Schema 展示气势轴、三维证据、格局候选、取用路径和来源分层。
- 新增独立 `almanac.html`、`almanac.js`、`almanac.css` 和 `/api/almanac`。
- 黄历独立于个人命盘，完整呈现日历信息和民俗历注，不与个人判断混杂。
- 现役静态资源使用版本缓存键，避免新旧 HTML/CSS/JS 混合。
- 三档专项排版验收按产品安排后置，本迭代只完成现有视口主流程验收。

## 0.2.0 变化摘要

- 增加身宫、未来五年流年、普通运年说明和太岁古籍参照。
- 每一步有效大运同时展示规则说明与《三命通会》古籍参照。
- 月令与日主古籍正文增加来源标签，避免与确定性计算混淆。
- 默认服务器和默认首页统一指向当前V3实现，不再保留旧入口的隐式北京经度行为。
- 强化JSON类型校验，并避免向浏览器泄露内部异常信息。
- 同步 CYBZ_reverse 已验证引擎、知识库和13项回归测试。
- 出生地点继续选填，真太阳时改为默认关闭的独立选项。
- 未启用真太阳时时不再向引擎传入默认北京经度。
- 启用校正后展示标准时间、真太阳时以及发生变化的四柱。
- Web请求测试现为7项，覆盖双历输入、两种时间口径和严格类型校验。
- 加载 `duanyu_structured.json`、`cn_classified.json`、`cn_pure.json` 和 `keys.txt`。
- 确定性匹配月令索引、天干五合和刑冲合会知识索引。
- 不将缺少选取规则的性格短语随机归属到个人命盘。
- 每柱直接展示天干/地支阴阳五行、生肖、藏干五行与藏干十神。
- 纳音、空亡、长生全部直接显示，不依赖悬浮提示。
- 五行按天干、地支、藏干分别统计，并显示合计。
- 展示全部有效大运及每一步对应的大运断语。
- 命理札记不再限制为前 12 条。

## 内部文件

- `bazi/knowledge_loader.py`：扩展知识加载和确定性匹配。
- `web_server.py`：唯一正式服务入口。
- `web_server_v3.py`：V3 请求处理层。
- `web_server_v2.py`：V2 输入与 API 适配层。
- `web/app_v2.js`：双历、地点和请求交互。
- `web/app_v3.js`：完整结果展示。
- `web/styles_v3.css`：完整结果样式。
- `bazi/web_schema.py`：实验推演与黄历的浏览器稳定数据契约。
- `web/almanac.html`、`web/almanac.js`、`web/almanac.css`：独立黄历体验。
