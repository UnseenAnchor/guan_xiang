---
version: alpha
name: 观象
description: 本地优先的四柱排盘与传统命理知识展示网页
colors:
  paper: "#efe7d7"
  paper-light: "#f8f2e7"
  ink: "#20231f"
  ink-soft: "#68675f"
  cinnabar: "#a93628"
  cinnabar-dark: "#79261e"
  jade: "#1d5c55"
  line: "rgba(35, 37, 32, .18)"
typography:
  serif:
    fontFamily: '"Noto Serif SC", "Songti SC", "SimSun", serif'
omitted:
  - spacing
  - rounded
  - components
---

## Overview

观象用于本地四柱排盘与传统命理知识展示。界面将排盘事实、古籍参照和实验推演分层呈现，不把知识参照包装为确定的人生结论。

## Colors

paper 承载页面底色，paper-light 用于浅色表面与深底反白文字；浅底正文使用 ink，辅助说明使用 ink-soft。

cinnabar 承担主操作与章节强调，cinnabar-dark 表达主操作悬停；jade 区分地点状态、来源标识与辅助操作，不把这些语义合并成单一强调色。

line 用于浅底卡片和条目分隔，深底四柱及实验评分区域保留各自的反白文字与边界处理。

## Typography

中文正文和表单沿用 serif 字体栈及其本机回退字体；书法字只用于现有装饰和展示角色，不替代正文与输入控件字体。

## Layout

公历与农历共用三段式日期输入的排版模式，切换历法时保留一致的字段顺序。

结果中的大运与流年沿用网格卡片排列，窄屏纵向展开。

手机结果页采用上方目录、下方正文的单列布局，目录链接可换行而不持续遮挡正文；较宽屏保留左侧目录。各端共用章节链接与选中态。

四柱保留年、月、日、时的阅读顺序；平板和手机允许横向滚动，不通过裁掉末柱或缩小数据字体适配窄屏，并提供可见的滚动提示与键盘焦点。

## Components

历法和性别的分段选择复用同一种选中态，选中与未选中的外观不能替代原生单选状态。

古籍正文使用可展开的参照区，实验推演默认折叠；展开后仍须显示来源和结论边界。

## Do's and Don'ts

- 保留原生 HTML、CSS、JavaScript 的无构建使用方式，不为套用外部 UI 规则迁移框架。
- 出生地选填，真太阳时默认关闭；选择地点不代表启用校正，界面说明必须与实际开关状态一致。
- 五行数量只称结构计数，条形图标为数量对比并说明按本盘最大计数归一化；不把条长当作总体百分比，也不将数量或条长等同于旺衰或喜用神。
- 分别标识历法计算、逆向确认、古籍参照与实验模型；知识索引命中不代表命理结论得到验证。
- 旺衰、格局、喜忌、用神、病药和通关不得宣称唯一正确，也不得默认作确定性吉凶判断；保留文化研究与娱乐体验的使用边界。
